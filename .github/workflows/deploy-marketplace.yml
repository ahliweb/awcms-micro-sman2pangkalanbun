name: Seed Marketplace Plugins

# Disabled — marketplace not configured for this deployment.
# To re-enable, uncomment the push trigger below and ensure
# the MARKETPLACE_SEED_TOKEN secret is configured.
on:
  workflow_dispatch:

#  push:
#    branches: [main]
#    paths:
#      - "packages/plugins/**"
#      - ".github/workflows/deploy-marketplace.yml"

permissions:
  contents: read

concurrency:
  group: seed-marketplace
  cancel-in-progress: false

jobs:
  seed:
    name: Seed Plugins
    runs-on: ubuntu-latest
    steps:
      - run: echo "Marketplace plugin publication is disabled for this deployment."
